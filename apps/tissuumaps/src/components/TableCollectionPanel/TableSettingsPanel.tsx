import { type Table } from "@tissuumaps/core";

type TableSettingsPanelProps = {
  tableId: string;
  table: Table;
};

export function TableSettingsPanel(
  // eslint-disable-next-line @typescript-eslint/no-unused-vars
  _props: TableSettingsPanelProps,
) {
  return <></>;
}
