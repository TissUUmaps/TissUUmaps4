import { IImageModel } from "../../models/image";

type ImageSettingsPanelProps = {
  imageId: string;
  image: IImageModel;
};

export default function ImageSettingsPanel(
  // eslint-disable-next-line @typescript-eslint/no-unused-vars
  _props: ImageSettingsPanelProps,
) {
  return <></>;
}
