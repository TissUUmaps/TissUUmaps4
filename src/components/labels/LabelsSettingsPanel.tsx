import { ILabelsModel } from "../../models/labels";

type LabelsSettingsPanelProps = {
  labelsId: string;
  labels: ILabelsModel;
};

export default function LabelsSettingsPanel(
  // eslint-disable-next-line @typescript-eslint/no-unused-vars
  _props: LabelsSettingsPanelProps,
) {
  return <></>;
}
