diff --git a/build/openseadragon/openseadragon.js b/build/openseadragon/openseadragon.js
index 57c89f14a58fdfbea8f2aadf1848118c11b7fe9a..3361474a896a89cb7cea73ba55d09569fbbe54bd 100644
--- a/build/openseadragon/openseadragon.js
+++ b/build/openseadragon/openseadragon.js
@@ -32772,6 +32772,11 @@ $.extend($.TiledImage.prototype, $.EventSource.prototype, /** @lends OpenSeadrag
             }
 
             const cache = options.cache;
+            // The unload above already decremented for the consumer it removed, so the replacement has
+            // to be counted here. Guarded, so that the error path above cannot count the same key twice.
+            if (!this._cachesLoaded[targetKey]) {
+                this._cachesLoadedCount++;
+            }
             this._cachesLoaded[targetKey] = cache;
             cache._ownerTileCache = this;
             cache.cacheKey = targetKey;
